<h1>Editing user</h1>

<% form_for(@user) do |f| %>
  <%= f.error_messages %>

 <%= render 'form', :f => f %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', root_url%>