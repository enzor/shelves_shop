<h1>New promotion</h1>

<% form_for(@promotion) do |f| %>
  <%= f.error_messages %>

  <%= render 'form' , :f => f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', promotions_path %>