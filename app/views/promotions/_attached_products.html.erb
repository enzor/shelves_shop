<div id="empty_product_container" class="empty_container">
	<% object.products.each do |c|%>
	<p>
		<select name="<%= "#{name}[product_ids][]" %>">
			<%= options_from_collection_for_select(Product.all(:order => 'name ASC'), "id","name",c.id)%>
		</select>
			<%= link_to "Remove",nil, :onclick => "$(this).parent().fadeOut('slow', function() { $(this).remove()});return false;", :class => 'button'%>
	<% end %>
	</p>
	<p>
	</p>
</div>


<div id="product_container" class="container_box" style="display:none">
 <p>

	<select name="<%= "#{name}[product_ids][]" %>">
		<option value=""></option>
		<%= options_from_collection_for_select(Product.all(:order => 'name ASC'), "id","name")%>
	</select>
		<%= link_to "Remove",nil, :onclick => "$(this).parent().fadeOut('slow', function() { $(this).remove()});return false;", :class => 'button'%>
 </p>
</div>

<p>
	<%= link_to "Add a Product",nil, :class => 'button', :onclick => "addEmptyProduct();return false"%>
</p>
